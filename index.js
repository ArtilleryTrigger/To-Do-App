const inputTask = document.getElementById("input--task");
const btn = document.getElementById("btn");
const task = document.querySelector(".task");

btn.onclick = function (){
    task.innerHTML += <button></button>
}